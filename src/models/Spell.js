export default class Spell {
  // eslint-disable-next-line camelcase
  constructor({ id, overview, title }) {
    this.id = id
    // eslint-disable-next-line camelcase
    this.overview = overview
    // eslint-disable-next-line camelcase
    this.title = title
  }
}