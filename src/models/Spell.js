export default class Spell {
  // eslint-disable-next-line camelcase
  constructor({ id, name }) {
    this.id = id
    // eslint-disable-next-line camelcase

    // eslint-disable-next-line camelcase
    this.name = name
  }
}